/**
 * Placeholder — Fastify listen entry arrives in Phase 3.
 * Keeping the file so `tsx`/`tsc` paths stay stable.
 */
console.log('@teslapool/api: database package ready (server arrives in Phase 3)');
